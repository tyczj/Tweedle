language: android
jdk: oraclejdk8
dist: trusty

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.android/build-cache

android:
  components:
    - build-tools-30.0.2
    - android-30
    - extra-google-m2repository
    - extra-android-m2repository

script:
  - ./gradlew :lib:clean :lib:build :lib:bintrayUpload -x test